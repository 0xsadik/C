// একটি ত্রিভুজের ক্ষেত্রফল নির্নয় 

#include<stdio.h>
int main()
{
    int b, h, area;
    scanf("%d", &b);
    scanf("%d", &h);

    area = .5 * b * h;
    printf("Area is: %d\n", area);

    return 0;
}